import { ArtistaResponseDto } from './artista-response.dto';

describe('ArtistaResponseDto', () => {
  it('should create an instance', () => {
    expect(new ArtistaResponseDto()).toBeTruthy();
  });
});
