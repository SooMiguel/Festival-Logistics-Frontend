import { EquipoResponseDto } from './equipo-response.dto';

describe('EquipoResponseDto', () => {
  it('should create an instance', () => {
    expect(new EquipoResponseDto()).toBeTruthy();
  });
});
