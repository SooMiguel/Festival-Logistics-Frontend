import { EscenarioResponseDto } from './escenario-response.dto';

describe('EscenarioResponseDto', () => {
  it('should create an instance', () => {
    expect(new EscenarioResponseDto()).toBeTruthy();
  });
});
