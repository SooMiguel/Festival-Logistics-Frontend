import { EscenarioRequestDto } from './escenario-request.dto';

describe('EscenarioRequestDto', () => {
  it('should create an instance', () => {
    expect(new EscenarioRequestDto()).toBeTruthy();
  });
});
