import { PresentacionResponseDto } from './presentacion-response.dto';

describe('PresentacionResponseDto', () => {
  it('should create an instance', () => {
    expect(new PresentacionResponseDto()).toBeTruthy();
  });
});
